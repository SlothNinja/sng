{{$g := $.Game}}
{{$cu := $.CUser}}
{{$cp := $g.CurrentPlayer}}
<div>
        {{template "atf/player_workers" data "g" $g "cu" $cu "p" $cp}}
        {{template "shared/flashbox" .}}
</div>
{{template "atf/select_area_to_place_worker_dialog"}}
